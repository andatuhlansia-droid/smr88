
<!DOCTYPE html>
<html amp lang="id-ID">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="color-scheme" content="dark">
    <title>SLOT777 - Samosir88 Link Gacor Situs Slot777 Online Gampang Menang Hari Ini Resmi</title>
    <meta name="description"
        content="Rasakan sensasi menang besar hari ini di SLOT777! Kunjungi link gacor resmi situs Slot777 online di Samosir88. Daftar sekarang dan amankan jackpotmu!" />
    <link rel="canonical" href="https://luxeride.com.my/about-us/" />
    <link rel="shortcut icon" href="https://ik.imagekit.io/bfpdttkyj/yaroslot777.jpg" />
    <link rel="preconnect" href="https://cdn.ampproject.org/">
    <link rel="preload" as="script" href="https://cdn.ampproject.org/v0.js">
    <script async src="https://cdn.ampproject.org/v0.js"></script>
    
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;600;700;800&family=Orbitron:wght@700;900&display=swap" rel="stylesheet">

    <style amp-custom>
/* === RESET & VARS (Cyberpunk Neon Theme) === */
:root {
    --banner-gold: #ffffff;       /* Pure White Accent */
    --banner-gold-dark: #333333;  /* Dark Gray Accent */
    --banner-cyan: #888888;       /* Light Gray Accent */
    --bg-gradient: linear-gradient(180deg, #121212 0%, #0a0a0a 50%, #000000 100%);
}

* { margin: 0; padding: 0; box-sizing: border-box; }

body {
    font-family: 'Poppins', sans-serif;
    background: var(--bg-gradient);
    color: #ffffff;
    min-height: 100vh;
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    overflow-x: hidden;
}

a { text-decoration: none; color: inherit; transition: all 0.3s cubic-bezier(0.25, 0.8, 0.25, 1); }

/* === CONTAINER === */
#container {
    width: 100%;
    max-width: 440px;
    padding: 30px 20px;
    position: relative;
    z-index: 2;
}

/* === LOGO === */
.logo-wrapper {
    text-align: center;
    margin-bottom: 25px;
}

.logo-img {
    filter: drop-shadow(0 0 15px rgba(255, 255, 255, 0.2));
}

/* === HEADER === */
.header-section {
    border-bottom: 2px dashed rgba(255, 255, 255, 0.2);
    padding-bottom: 18px;
    margin-bottom: 25px;
    text-align: center;
}

h1.title {
    font-family: 'Orbitron', sans-serif;
    font-weight: 900;
    font-size: 18px;
    text-transform: uppercase;
    background: linear-gradient(90deg, #ffffff, #888888, #ffffff);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    text-shadow: 0 0 20px rgba(255, 255, 255, 0.3);
    line-height: 1.4;
    letter-spacing: 1.5px;
}

/* === HERO CARD === */
.hero-card {
    border-radius: 20px;
    overflow: hidden;
    border: 2px solid rgba(255, 255, 255, 0.2);
    box-shadow: 0 0 35px rgba(0, 0, 0, 0.8), inset 0 0 15px rgba(255, 255, 255, 0.05);
    margin-bottom: 30px;
    position: relative;
    background: #000;
}

.hero-card::after {
    content: '';
    position: absolute;
    top: 0; left: -100%;
    width: 60%; height: 100%;
    background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.15), transparent);
    transform: skewX(-25deg);
    animation: shine 4s cubic-bezier(0.4, 0, 0.2, 1) infinite;
}

@keyframes shine {
    0% { left: -150%; }
    20% { left: 150%; }
    100% { left: 150%; }
}

/* === BUTTONS === */
.btn-list {
    display: flex;
    flex-direction: column;
    gap: 15px;
}

.btn-grid {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 15px;
}

.btn {
    display: flex;
    align-items: center;
    justify-content: center;
    padding: 16px 15px;
    border-radius: 14px;
    font-weight: 800;
    font-size: 15px;
    text-transform: uppercase;
    letter-spacing: 1px;
    text-align: center;
    position: relative;
}

/* Tombol Utama (Ultra Glow Shimmer) */
.btn-main {
    background: linear-gradient(90deg, #1a1a1a, #444444, #222222, #1a1a1a);
    background-size: 300% auto;
    color: #ffffff;
    animation: shimmer 4s linear infinite;
    border: 1px solid rgba(255,255,255,0.2);
    box-shadow: 0 0 25px rgba(0, 0, 0, 0.6);
    text-shadow: 0 2px 4px rgba(0,0,0,0.8);
    font-family: 'Orbitron', sans-serif;
}

/* Tombol Sub (Glassmorphism Cyber) */
.btn-sub {
    background: rgba(20, 20, 20, 0.6);
    color: #ffffff;
    border: 1px solid rgba(255, 255, 255, 0.2);
    backdrop-filter: blur(8px);
    box-shadow: 0 4px 15px rgba(0,0,0,0.6), inset 0 0 10px rgba(255, 255, 255, 0.05);
    text-shadow: 0 0 8px rgba(255, 255, 255, 0.4);
}

@keyframes shimmer {
    0% { background-position: 0% center; }
    100% { background-position: 300% center; }
}

.btn:hover {
    transform: translateY(-3px) scale(1.02);
}

.btn-main:hover {
    box-shadow: 0 0 35px rgba(255, 255, 255, 0.2), 0 0 15px rgba(0, 0, 0, 0.7);
}

.btn-sub:hover {
    border-color: var(--banner-gold);
    box-shadow: 0 0 20px rgba(255, 255, 255, 0.2);
    background: rgba(255, 255, 255, 0.1);
    text-shadow: 0 0 8px rgba(255, 255, 255, 0.6);
}

/* === FOOTER === */
.footer {
    margin-top: 40px;
    font-size: 11px;
    color: #888888;
    text-align: center;
    line-height: 1.8;
}

.footer a { color: var(--banner-cyan); font-weight: 700; text-shadow: 0 0 8px rgba(255, 255, 255, 0.2); }
.footer a:hover { color: var(--banner-gold); }

.footer hr {
    border: 0;
    border-top: 1px dashed rgba(255, 255, 255, 0.2);
    margin: 15px 0;
}

.footer b {
    color: #ffffff;
    letter-spacing: 2px;
    font-family: 'Orbitron', sans-serif;
    text-shadow: 0 0 10px rgba(255, 255, 255, 0.5);
}

/* === BACKGROUND GLOW === */
.bg-particles {
    position: fixed;
    top: 0; left: 0; width: 100%; height: 100%;
    z-index: 1;
    background-image: 
        radial-gradient(circle at 50% 20%, rgba(255, 255, 255, 0.05) 0%, transparent 60%),
        radial-gradient(circle at 50% 80%, rgba(0, 0, 0, 0.5) 0%, transparent 60%);
    pointer-events: none;
}
</style>

    <style amp-boilerplate>body{-webkit-animation:-amp-start 8s steps(1,end) 0s 1 normal both;-moz-animation:-amp-start 8s steps(1,end) 0s 1 normal both;-ms-animation:-amp-start 8s steps(1,end) 0s 1 normal both;animation:-amp-start 8s steps(1,end) 0s 1 normal both}@-webkit-keyframes -amp-start{from{visibility:hidden}to{visibility:visible}}@-moz-keyframes -amp-start{from{visibility:hidden}to{visibility:visible}}@-ms-keyframes -amp-start{from{visibility:hidden}to{visibility:visible}}@-o-keyframes -amp-start{from{visibility:hidden}to{visibility:visible}}@keyframes -amp-start{from{visibility:hidden}to{visibility:visible}}</style><noscript><style amp-boilerplate>body{-webkit-animation:none;-moz-animation:none;-ms-animation:none;animation:none}</style></noscript>
</head>

<body>

    <div class="bg-particles"></div>

    <div id="container">
        
        <!-- Logo Section -->
        <div class="logo-wrapper">
            <amp-img 
                src="https://ik.imagekit.io/bfpdttkyj/ku.webp?updatedAt=1780296190798"
                alt="BURUNG303" 
                width="161" height="29" 
                layout="intrinsic"
                class="logo-img">
            </amp-img>
        </div>

        <!-- Header Title Section -->
        <div class="header-section">
            <h1 class="title">LINK GACOR HARI INI</h1>
        </div>

        <!-- Banner Card -->
        <div class="hero-card">
            <amp-img 
                src="https://ik.imagekit.io/bfpdttkyj/yaroslot777.jpg"
                alt="BURUNG303" 
                width="598" height="598" 
                layout="responsive">
            </amp-img>
        </div>

        <!-- Button Section -->
        <div class="btn-list">
            <!-- Tombol Utama (Warna Emas Banner) -->
            <a href="https://ulosbatak.life/ebailaconm" class="btn btn-main">
                MASUK / LOGIN &nbsp;►
            </a>
            
            <!-- Tombol Alternatif (Warna Navy + Cyan Border) -->
            <div class="btn-grid">
                <a href="https://ulosbatak.life/ebailaconm" class="btn btn-sub">
                    ALTERNATIF 1
                </a>
                <a href="https://ulosbatak.life/ebailaconm" class="btn btn-sub">
                    ALTERNATIF 2
                </a>
            </div>
        </div>

        <!-- Footer Section -->
        <div class="footer">
            <p>© 2026 <a href="https://amp-slot.web.app/BURUNG303/">SLOT777</a>. All Rights Reserved.</p>
            <hr>
            <b>Disclaimer • PEMBURU JANDA PIRANG</b>
        </div>

    </div>

</body>
</html>
